// import {Table} from '@tanstack/react-table';
// import {ReactElement, createContext} from 'react';

// export interface TableContextType<T> {
//   table: Table<T>;
// }

// // eslint-disable-next-line @typescript-eslint/no-explicit-any
// export const TableContext = createContext<TableContextType<any> | undefined>(undefined);

// interface TableProviderProps<T> {
//   children: ReactElement;
//   table: Table<T>;
// }

// export const TableProvider = <T,>({children, table}: TableProviderProps<T>) => {
//   return <TableContext.Provider value={{table}}>{children}</TableContext.Provider>;
// };
